public Customer findById(String customerId) {
    Customer c = null;
    String sql = "SELECT * FROM customer WHERE customer_id = ?";
    try (Connection conn = getConnection();
         PreparedStatement ps = conn.prepareStatement(sql)) {

        ps.setString(1, customerId);
        ResultSet rs = ps.executeQuery();

        if (rs.next()) {
            c = new Customer();
            c.setCustomerId(rs.getString("customer_id"));
            c.setCustomerName(rs.getString("customer_name"));
            c.setSex(rs.getInt("sex"));
            c.setBirthday(rs.getDate("birthday"));
            // Thêm các cột khác (address, phone...) nếu có
        }

    } catch (Exception e) {
        e.printStackTrace();
    }
    return c;
}


public Customer findById(String customerId) {
    Customer c = null;
    String sql = "SELECT * FROM customer WHERE customer_id = ?";
    try (Connection conn = getConnection();
         PreparedStatement ps = conn.prepareStatement(sql)) {

        ps.setString(1, customerId);
        ResultSet rs = ps.executeQuery();

        if (rs.next()) {
            c = new Customer();
            c.setCustomerId(rs.getString("customer_id"));
            c.setCustomerName(rs.getString("customer_name"));
            c.setSex(rs.getInt("sex"));
            c.setBirthday(rs.getDate("birthday"));
            // Thêm các cột khác (address, phone...) nếu có
        }

    } catch (Exception e) {
        e.printStackTrace();
    }
    return c;
}


https://ibytecode.com/blog/pagination-in-servlet-and-jsp/

https://www.tutorialspoint.com/jsp/jsp_session_tracking.htm
https://www.studytonight.com/servlet/pagination-example-in-servlet.php
https://coderanch.com/t/352097/java/checkbox-jsp
https://www.baeldung.com/java-servlet-cookies-session
https://cystack.net/vi/tutorial/quan-ly-session-trong-java
https://hanam88.com/kho-tai-lieu/63/240/java-web-loc-phan-trang-tim-kiem-du-lieu-voi-sql-server-trong-jsp-servlet.html?utm_source=chatgpt.com